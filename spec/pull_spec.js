//var $ = require('jquery');
//This component is untested
/*
describe('pull and save', function() {
  it('should pull from the registered url and save the returned object to the local store', function() {
    var flag, storedVal, retrievedVal, didMatch;
    runs(function() {
      //uses the object from register_spec
      pull('slTestObjectRegister', function(err, data) {
        if (!err) {
          storedVal = data;
        } else {
          storedVal = 'error in pull';
        }
        flag = true;
      });
    });
    waitsFor(function() {
      return flag;
    }, 'flag should be true', 5000);
    runs(function() {
      expect(flag).toBe(true);
      expect(storedVal).toBe(true);
    });
  });
    
});
*/